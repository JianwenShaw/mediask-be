---
alwaysApply: false
description: Java开发规范代码规范 DDD领域驱动设计分层架构 架构设计命名规范 JDK21特性 高内聚低耦合 应用服务ApplicationService 领域服务DomainService 基础设施层Infrastructure 数据访问层DAL Request DTO VO DO 工具类Utils 助手类Helper 代码风格 最佳实践
---

## 项目架构

### 分层架构（DDD）

本项目采用 **DDD（领域驱动设计）分层架构**，模块划分如下：

```
mediask-api          # API接入层 - REST控制器、请求响应、参数校验
mediask-service      # 应用服务层 - 业务编排、用例实现、Request/DTO转换
mediask-domain       # 领域层 - 实体、值对象、领域服务、仓储接口（核心业务逻辑）
mediask-infra        # 基础设施层 - 仓储实现、缓存、分布式锁、文件存储、AI客户端
mediask-dal          # 数据对象层 - DO实体、Mapper接口定义
mediask-common       # 通用层 - 工具类、常量、异常、统一响应
```

### 依赖关系规则（严格遵循）

```
API → Service → Domain
     ↓         ↓
   Common   Infra → Domain, DAL, Common
              ↓
            DAL → Common
```

**关键约束**：
- ✅ API 层只能依赖 Service 层和 Common 层
- ✅ Service 层只能依赖 Domain 层、Infra 层和 Common 层
- ✅ Domain 层只能依赖 Common 层（核心业务逻辑，不依赖基础设施）
- ✅ Infra 层实现 Domain 层的仓储接口，依赖 Domain、DAL、Common
- ✅ DAL 层只包含 DO 和 Mapper，依赖 Common 层
- ❌ **禁止跨层依赖**（如 API 直接依赖 Domain 或 Infra）
- ❌ **禁止循环依赖**

## 命名规范（强制执行）

### 应用层命名

| 层级 | 类型 | 命名规范 | 示例 |
|------|------|---------|------|
| **API 层** | HTTP 请求对象 | `XxxRequest` | `LoginRequest`, `RegisterRequest` |
| **API 层** | HTTP 响应对象 | `XxxResponse` | `LoginResponse`, `CurrentUserResponse` |
| **Service 层** | 应用层请求对象 | `XxxRequest` | `LoginRequest`, `CreateScheduleRequest` |
| **Service 层** | 应用层响应对象 | `XxxDTO` | `LoginResponseDTO`, `CurrentUserDTO` |
| **Domain 层** | 领域服务 | `XxxDomainService` | `AutoScheduleDomainService` |
| **Domain 层** | 领域实体 | 业务名称 | `DoctorSchedule`, `AppointmentSlot` |
| **Domain 层** | 值对象 | 业务名称 | `DoctorId`, `TimePeriod`, `ScheduleStatus` |
| **DAL 层** | 数据对象 | `XxxDO` | `UserDO`, `DoctorScheduleDO` |
| **DAL 层** | Mapper | `XxxMapper` | `UserMapper`, `DoctorScheduleMapper` |
| **Infra 层** | 仓储实现 | `XxxRepositoryImpl` | `DoctorScheduleRepositoryImpl` |
| **Infra 层** | 转换器 | `XxxConverter` | `ScheduleConverter` |
| **Common 层** | 工具类 | `XxxUtils` | `DateUtils`, `StringUtils` |
| **Common 层** | 助手类 | `XxxHelper` | `EncryptHelper`, `ValidationHelper` |

### 通用对象类型说明

| 类型 | 全称 | 用途 | 命名规范 | 示例 |
|------|------|------|---------|------|
| **DTO** | Data Transfer Object | 在各层之间传递数据的对象，不包含业务逻辑 | `XxxDTO` | `LoginResponseDTO`, `UserInfoDTO` |
| **VO** | View Object | 用于用户界面层向应用层传递数据，本质也是一种DTO。**注意：不要和DDD的Value Object值对象搞混** | `XxxVO` | `UserInfoVO`, `DoctorDetailVO` |
| **DO** | Data Object | 数据库表映射对象（本项目使用DO，不使用PO） | `XxxDO` | `UserDO`, `DoctorScheduleDO` |
| **PO** | Persistence Object | 持久化对象（传统命名，本项目统一使用DO） | - | - |
| **Utils** | - | 工具类，提供静态方法 | `XxxUtils` | `DateUtils`, `StringUtils` |
| **Helper** | - | 助手类，帮助完成某类操作的工具类 | `XxxHelper` | `EncryptHelper`, `ValidationHelper` |

### 重要说明

1. **Request vs Command**：
   - ✅ 使用 `Request`（更符合传统 DDD 实践）
   - ❌ 不使用 `Command`（除非明确采用 CQRS 模式）

2. **DTO vs VO**：
   - Service 层输出使用 `DTO`（Data Transfer Object）
   - API 层输出使用 `Response` 或 `VO`（View Object）
   - **注意**：VO 是 View Object，不要和 DDD 的 Value Object（值对象）混淆

3. **DO vs PO**：
   - 本项目统一使用 `DO`（Data Object）作为数据库表映射对象
   - 不使用 `PO`（Persistence Object）命名
   - DO 放在 DAL 层，使用 MyBatis-Plus 注解

4. **Utils vs Helper**：
   - `Utils`：提供静态方法的工具类，通常是无状态的
   - `Helper`：助手类，可能包含状态或更复杂的逻辑
   - 两者都放在 Common 层

5. **领域服务命名**：
   - 领域服务使用 `XxxDomainService` 后缀，与应用服务区分
   - 应用服务使用 `XxxApplicationService` 后缀

## 代码风格

### 参考文档

请严格参考代码风格文档：
- `/home/ethan/dev/mediask-be/MediAskDocs/docs/02-CODE_STANDARDS.md`

### 核心原则

1. **命名清晰**：变量、方法、类名必须清晰表达业务含义，禁止缩写和拼音
2. **单一职责**：每个类、方法只做一件事
3. **高内聚低耦合**：相关功能聚合，模块间依赖最小化
4. **优雅简洁**：代码应该易读、易维护，避免过度设计

## JDK 21 特性使用指南

### 推荐使用的特性

1. **Record 类**：用于不可变数据传输对象
   ```java
   public record LoginResponseDTO(Long userId, String username, String token) {}
   ```

2. **Pattern Matching**：switch 表达式和模式匹配
   ```java
   return switch (userType) {
       case ADMIN -> List.of("schedule:create", "schedule:update");
       case DOCTOR -> List.of("schedule:update");
       case PATIENT -> Collections.emptyList();
   };
   ```

3. **Text Blocks**：多行字符串
   ```java
   String sql = """
       SELECT * FROM users
       WHERE id = ?
       """;
   ```

4. **Sealed Classes**：限制类继承（用于值对象或枚举扩展）

5. **var 关键字**：局部变量类型推断（适度使用，保持可读性）

### 虚拟线程注意事项 ⚠️

1. **数据库连接池**：
   - MySQL 驱动在虚拟线程环境下可能性能下降
   - 使用传统线程池处理数据库操作，避免虚拟线程
   - 连接池大小需要根据实际情况调整

2. **I/O 密集型操作**：
   - 文件操作、网络请求可以使用虚拟线程
   - 但需要验证实际性能表现

3. **原则**：**性能优先，不盲目使用新特性**

## DDD 开发守则

### 领域层（Domain Layer）

1. **实体（Entity）**：
   - 包含业务标识和业务逻辑
   - 通过工厂方法或构造函数创建
   - 封装业务规则，不暴露内部状态

2. **值对象（Value Object）**：
   - 不可变，通过值相等判断
   - 使用 Record 或 final class 实现

3. **领域服务（Domain Service）**：
   - 处理跨实体的业务逻辑
   - 命名：`XxxDomainService`
   - 不依赖基础设施，只依赖领域对象和 Common

4. **仓储接口（Repository Interface）**：
   - 定义在 Domain 层
   - 使用领域对象作为参数和返回值
   - 实现放在 Infra 层

### 应用层（Application Layer）

1. **应用服务（Application Service）**：
   - 协调领域服务和仓储完成用例
   - 管理事务边界
   - 处理 Request/DTO 转换
   - 命名：`XxxApplicationService`

2. **Request/DTO**：
   - Request：应用层输入参数（`XxxRequest`）
   - DTO：应用层输出对象（`XxxDTO`）
   - 放在 `application/request` 和 `application/dto` 包下

### 基础设施层（Infrastructure Layer）

1. **仓储实现**：
   - 实现 Domain 层的仓储接口
   - 使用 MyBatis-Plus 或 JPA
   - DO 和领域对象的转换

2. **基础设施组件**：
   - Redis 配置、分布式锁
   - 文件存储、AI 客户端
   - JWT 服务、密码编码器
   - 所有技术实现细节

### 数据对象层（DAL Layer）

1. **DO（Data Object）**：
   - 数据库表映射对象（等同于传统意义上的 PO）
   - 使用 MyBatis-Plus 注解
   - 命名：`XxxDO`
   - **注意**：本项目统一使用 DO，不使用 PO 命名

2. **Mapper**：
   - MyBatis Mapper 接口
   - 只包含数据访问方法
   - 不包含业务逻辑

### 通用层（Common Layer）

1. **工具类（Utils）**：
   - 提供静态方法的工具类
   - 通常是无状态的
   - 命名：`XxxUtils`
   - 示例：`DateUtils`, `StringUtils`, `CollectionUtils`

2. **助手类（Helper）**：
   - 帮助完成某类操作的工具类
   - 可能包含状态或更复杂的逻辑
   - 命名：`XxxHelper`
   - 示例：`EncryptHelper`, `ValidationHelper`, `FileHelper`

## Spring Security 组件分层

1. **API 层**：
   - `SecurityConfig` - Web 安全配置
   - `JwtAuthenticationFilter` - JWT 认证过滤器
   - 异常处理器（`JwtAuthenticationEntryPoint`、`JwtAccessDeniedHandler`）

2. **Infra 层**：
   - `PasswordEncoderConfig` - 密码编码器配置
   - `JwtService` - JWT 服务
   - `JwtProperties` - JWT 配置属性

3. **Service 层**：
   - 通过依赖注入使用 `PasswordEncoder`（不直接依赖 Spring Security）

## 代码质量要求

1. **异常处理**：
   - 使用项目统一的异常体系（`BizException`）
   - 不吞异常，记录日志
   - 异常信息清晰，便于排查

2. **日志规范**：
   - 使用 SLF4J + Logback
   - 关键业务操作记录 INFO 日志
   - 异常记录 ERROR 日志，包含上下文信息

3. **事务管理**：
   - 在应用服务方法上使用 `@Transactional`
   - 明确指定 `rollbackFor = Exception.class`
   - 避免在领域对象中直接使用事务注解

4. **参数校验**：
   - API 层使用 `@Valid` 或 `@Validated`
   - Service 层使用 `AssertUtil` 进行业务校验
   - 校验失败抛出明确的业务异常

5. **空值处理**：
   - 使用 `Optional` 处理可能为空的值
   - 避免 `null` 传递，使用空对象模式或明确处理

## 性能优化原则

1. **数据库查询**：
   - 避免 N+1 查询问题
   - 合理使用索引
   - 批量操作使用批量方法

2. **缓存策略**：
   - 热点数据使用 Redis 缓存
   - 注意缓存穿透、击穿、雪崩问题
   - 使用分布式锁保证数据一致性

3. **对象转换**：
   - 使用 MapStruct 进行对象转换（编译时生成，性能高）
   - 避免手动 getter/setter 转换

## 禁止事项

1. ❌ **禁止在 Domain 层使用 Spring 注解**（除了 `@Service` 用于领域服务）
2. ❌ **禁止在 Domain 层直接依赖 DAL 层**
3. ❌ **禁止在 Service 层直接使用 Mapper**（通过 Repository 接口）
4. ❌ **禁止在 API 层直接调用 Domain 层**
5. ❌ **禁止使用缩写和拼音命名**
6. ❌ **禁止在循环中进行数据库查询**
7. ❌ **禁止忽略异常或使用空的 catch 块**

## 最佳实践

1. **优先使用组合而非继承**
2. **使用依赖注入，避免硬编码依赖**
3. **保持方法简短，单一职责**
4. **使用有意义的变量名和方法名**
5. **及时重构，保持代码整洁**
6. **编写清晰的注释，解释"为什么"而非"是什么"**
